---
import BaseLayout from "../../layouts/BaseLayout.astro"; import SiteHeader from "../../components/SiteHeader.astro"; import SiteFooter from "../../components/SiteFooter.astro"; import ContactCta from "../../components/ContactCta.astro"; import { services } from "../../data/services";
---
<BaseLayout title="Services | Glavas Engineering"><SiteHeader /><main><section class="section"><div class="container"><p class="eyebrow">Services</p><h1>Thermal analysis for the building envelope.</h1><div class="service-list">{services.map((service, i) => <article><span>0{i + 1}</span><h2>{service.title}</h2><p>{service.intro}</p><ContactCta label="Read more" href={`/services/${service.slug}`} event="service_view" location="service_list" /></article>)}</div></div></section></main><SiteFooter /></BaseLayout>
<style>
  h1 { max-width: 52rem; margin-bottom: 6rem; } .service-list { border-top:1px solid var(--color-light-grey); } .service-list article { display:grid; grid-template-columns:4rem 1fr 1.2fr auto; gap:1.5rem; align-items:start; padding:1.5rem 0; border-bottom:1px solid var(--color-light-grey); } .service-list article>span { color:var(--color-teal); font-size:.875rem; font-weight:500; } .service-list h2 { margin:0; font-size:clamp(1.4rem,2.5vw,2.25rem); } .service-list p { margin:0; color:var(--color-slate); font-size:1rem; } .service-list .contact-cta { white-space:nowrap; } @media(max-width:44rem){ h1{margin-bottom:3rem}.service-list article{grid-template-columns:2.5rem 1fr;gap:.5rem 1rem}.service-list p,.service-list .contact-cta{grid-column:2}.service-list .contact-cta{justify-self:start;margin-top:.5rem} }
</style>
